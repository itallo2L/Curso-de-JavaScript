var velocidade = 85;
console.log(`A velocidade do seu carro é ${velocidade}km/h`);
if (velocidade > 60) { /*Condição simples (Se a velocidade 
  for maior que 60 execute o código abaixo)*/
  console.log(`Você ultrapassou a velocidae permitida. MULTADO!!`);
}
console.log(`Dirija sempre usando o cinto de segurança!`);